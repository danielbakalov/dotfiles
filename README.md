# dotfiles for my macbook

desktop: aerospace + sketchybar

terminal: kitty + tmux

shell: zsh + starship

editors: nvim (code), doom (org)

tools: git, ssh, fastfetch

theme: kanagawa dragon

disclaimer: don't copy these exactly as they are designed specifically for me.
